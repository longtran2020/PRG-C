#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int x = 4, y = 6, celkem;
    celkem = 2 * (x * y) + 2;
    if (celkem < 46)
    {
        printf("yes");
    }
    else if (celkem => 46);
    {
        printf("nope");
    }
    
    return 0;
}